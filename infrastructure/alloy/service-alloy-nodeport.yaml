apiVersion: v1
kind: Service
metadata:
  name: alloy-ui-nodeport
  namespace: monitoring
  annotations:
    argocd.argoproj.io/sync-wave: "10"
  labels:
    app.kubernetes.io/name: alloy
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: alloy
  ports:
    - name: ui
      port: 12345
      targetPort: 12345
      nodePort: 31234
