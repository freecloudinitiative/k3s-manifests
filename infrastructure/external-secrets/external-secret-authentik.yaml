apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-config
  namespace: authentik
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao-store
  target:
    name: authentik-config
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      data:
        AUTHENTIK_POSTGRESQL__PASSWORD: "{{ .postgresqlPassword }}"
        AUTHENTIK_SECRET_KEY: "{{ .secretKey }}"
  data:
    - secretKey: postgresqlPassword
      remoteRef:
        key: authentik
        property: postgresql-password
    - secretKey: secretKey
      remoteRef:
        key: authentik
        property: secret-key
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-bootstrap
  namespace: authentik
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao-store
  target:
    name: authentik-bootstrap
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      data:
        AUTHENTIK_BOOTSTRAP__EMAIL: "{{ .bootstrapEmail }}"
        AUTHENTIK_BOOTSTRAP__PASSWORD: "{{ .bootstrapPassword }}"
  data:
    - secretKey: bootstrapEmail
      remoteRef:
        key: authentik
        property: bootstrap-email
    - secretKey: bootstrapPassword
      remoteRef:
        key: authentik
        property: bootstrap-password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-postgresql-credentials
  namespace: platform-database
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao-store
  target:
    name: authentik-postgresql-credentials
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      type: kubernetes.io/basic-auth
      data:
        username: authentik
        password: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: authentik
        property: postgresql-password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: platform-postgresql-credentials
  namespace: platform-database
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao-store
  target:
    name: platform-postgresql-credentials
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      type: kubernetes.io/basic-auth
      data:
        username: platform
        password: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: platform-postgresql
        property: password
