apiVersion: v1
kind: Service
metadata:
  name: policy-reporter-ui-external
  namespace: kyverno
  labels:
    app.kubernetes.io/name: policy-reporter-ui
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: policy-reporter-ui
  ports:
    - port: 8082
      targetPort: 8080
      name: http
